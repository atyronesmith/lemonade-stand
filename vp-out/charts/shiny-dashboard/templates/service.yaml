apiVersion: v1
kind: Service
metadata:
  name: shiny-dashboard
  labels:
    app: shiny-dashboard
spec:
  selector:
    app: shiny-dashboard
  ports:
    - name: http
      port: 3838
      targetPort: 3838
      protocol: TCP
