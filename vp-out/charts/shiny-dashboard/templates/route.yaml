apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: shiny-dashboard
  labels:
    app: shiny-dashboard
spec:
  to:
    kind: Service
    name: shiny-dashboard
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
