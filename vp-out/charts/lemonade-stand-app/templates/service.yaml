apiVersion: v1
kind: Service
metadata:
  name: lemonade-stand-app
  labels:
    app: lemonade-stand-app
spec:
  selector:
    app: lemonade-stand-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
