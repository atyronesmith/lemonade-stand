apiVersion: v1
kind: Service
metadata:
  name: chunker-service
  labels:
    app: chunker-service
spec:
  selector:
    app: chunker-service
  ports:
    - name: grpc
      port: 8085
      targetPort: 8085
      protocol: TCP
