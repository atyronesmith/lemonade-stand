# Generated by quickpat compose -- do not edit
apiVersion: v1
kind: ConfigMap
metadata:
  name: guardrails-config
data:
  config.yaml: |
    chunkers:
      sentence:
        hostname: {{ custom.chunker-service.endpoint }}
        port: 8085
    openai:
      service:
        hostname: {{ blocks.llm.output.predictor_host }}
    detectors:
      hap:
        hostname: {{ blocks.hap-detector.output.predictor_host }}
        port: 8000
      prompt_injection:
        hostname: {{ blocks.prompt-injection-detector.output.predictor_host }}
        port: 8000
      language_detection:
        hostname: {{ custom.lingua-detector.endpoint }}
        port: 8080
