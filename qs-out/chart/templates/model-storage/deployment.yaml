# Generated by quickpat compose -- do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
  name: model-storage
  labels:
    app: model-storage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: model-storage
  template:
    metadata:
      labels:
        app: model-storage
    spec:
      initContainers:
        - name: download-models
          image: quay.io/rgeada/llm_downloader:latest
          env:
            - name: MODELS_PATH
              value: /mnt/models/huggingface
            - name: MODEL_IDS
              value: 'ibm-granite/granite-guardian-hap-125m,protectai/deberta-v3-base-prompt-injection-v2'
          volumeMounts:
            - name: model-pvc
              mountPath: /mnt/models
      containers:
        - name: minio
          image: {{ .Values.modelStorage.image }}
          args:
            - server
            - /models
          env:
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: model-storage-credentials
                  key: AWS_ACCESS_KEY_ID
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: model-storage-credentials
                  key: AWS_SECRET_ACCESS_KEY
          ports:
            - containerPort: 9000
          volumeMounts:
            - name: model-pvc
              mountPath: /models
      volumes:
        - name: model-pvc
          persistentVolumeClaim:
            claimName: model-storage-pvc
